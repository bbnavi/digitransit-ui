version: '3.7'
# This file partially overrides stack.yml.
services:
  digitransit-ui:
    environment:
      - CONFIG=bbnavi-staging
      # don't inherit value from stack.yml, only use it if it has been defined explicitly
      - API_URL
    deploy:
      labels:
        traefik.frontend.rule: Host:staging.bbnavi.de
        # bbnavi:digitransit
        traefik.frontend.auth.basic.users: bbnavi:$$apr1$$s135n0lg$$.yhGUnsFgFVpKW.9UuyTJ/
